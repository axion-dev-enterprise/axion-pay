export const env = {
  appName: 'AXION-PAY',
  siteUrl:
    typeof window !== 'undefined' ? window.location.origin : 'https://pay.axionenterprise.cloud',
  docsUrl:
    typeof window !== 'undefined'
      ? `${window.location.origin}/docs`
      : 'https://pay.axionenterprise.cloud/docs',
}

